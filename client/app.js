import React from 'react';
import ReactDOM from 'react-dom';
import Chart from './Chart';
// let element = React.createElement(Chart, { container: 'chart', options: options });
ReactDOM.render(<Chart/>, document.getElementById('main'));
